{# templates/compte_rendu/edit.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Modifier Compte Rendu{% endblock %}

{% block body %}
    <h1>Modifier Compte Rendu</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-warning">Modifier</button>
    {{ form_end(form) }}
    <a href="{{ path('compte_rendu_index') }}" class="btn btn-secondary">Retour à la liste</a>
{% endblock %}